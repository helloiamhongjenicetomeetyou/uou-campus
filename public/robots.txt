# 울산대 캠퍼스 길찾기 — https://www.uou-campus.site
#
# 이 파일이 없으면 vercel.json 의 rewrite 가 /robots.txt 요청까지 index.html 로
# 돌려보낸다. 크롤러는 그 HTML 을 robots.txt 로 읽으려다 줄마다 오류를 내고,
# 라이트하우스 SEO 가 55개 오류로 92점에 묶인다. 정적 파일은 rewrite 보다 먼저
# 잡히므로, 여기 놓아 두면 그대로 나간다.

User-agent: *
Allow: /

# 지도를 고치는 화면과 그 저장 함수. 검색 결과에 뜰 이유가 없다.
Disallow: /api/
Disallow: /*?edit=1
