dist
node_modules
pnpm-lock.yaml
*.md
public

# 오버패스에서 받아 온 원본 덤프. 우리가 쓴 코드가 아니라 벤더 파일이다.
scripts/osm-campus.json

# 그래프는 생성물이다. 시드 스크립트와 /api/publish 가 같은 모양으로 쓰는데,
# Prettier 가 배열을 한 줄로 접어 두면 저장할 때마다 백 줄짜리 서식 차이가 난다.
# 그러면 '누가 뭘 고쳤나' 가 이력에서 안 보인다. 만든 쪽 모양 그대로 둔다.
src/data/campus.json
